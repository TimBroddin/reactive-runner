{
  "name": "reactive-runner",
  "version": "0.1.0",
  "description": "A reactive (auto)-runner inspired by Meteor Tracker",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["autorun", "dependencies", "runner", "reactive"],
  "author": "Tim Broddin",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0"
  }
}
